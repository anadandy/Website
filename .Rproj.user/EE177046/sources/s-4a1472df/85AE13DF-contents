#run to install the blogdown package; wait until finished
install.packages("blogdown")
#run to install hugo; wait until finished
blogdown::install_hugo()

blogdown::new_site(theme="nurlansu/hugo-sustain")

cd ~/stor/home/amd5355/Website #if you haven't already

git init #initialize local git respository
git add . #add all files in repository to be uploaded
git commit -m "first website commit"
git remote add origin https://github.com/anadandy/Website.git #re
git push origin master

set-url

library(ggplot2)
as.numeric(py$dog_height)
as.numeric(py$dog_weight)
mean(py$dog_height)
ggplot()+geom_point(aes(py$dog_height,py$dog_weight))
py$dog_height
py$dog_height['dachshund']